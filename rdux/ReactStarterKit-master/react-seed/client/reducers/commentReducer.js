export default function comments(state=[],action){
    switch(action.type){
        case 'ADD_COMMENT':
            
            return state;
        default:
            console.log("within comments reducer!");
            return state;
}
}

//when
